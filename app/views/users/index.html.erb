<h1 class="title">All Users</h1>

<h4 class="title is-4"><%= pluralize(@users.size, "user") %></h4>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to user.username, user_path(user) %>
    </li>
  <% end %>
</ul>